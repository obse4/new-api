# Fork 项目同步与合并指南

本文档提供了一套完整的操作流程，用于定期从上游仓库同步代码并合并到你的 fork 项目中，同时保留你的二次开发内容。

## 📋 目录

- [仓库信息](#仓库信息)
- [初始设置](#初始设置)
- [日常同步流程](#日常同步流程)
- [冲突解决](#冲突解决)
- [测试与验证](#测试与验证)
- [部署上线](#部署上线)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)

## 🔗 仓库信息

根据你的 Git 配置，当前仓库信息如下：

- **你的 Fork 仓库（origin）**: `git@github.com:obse4/new-api.git`
- **上游仓库（upstream）**: `https://github.com/QuantumNous/new-api.git`
- **主分支**: `main`

> 💡 **提示**: 如果上游仓库地址不对，请参考[初始设置](#初始设置)部分进行配置。

## 🚀 初始设置

### 1. 检查远程仓库配置

```bash
# 查看所有远程仓库
git remote -v
```

应该看到类似输出：
```
origin    git@github.com:obse4/new-api.git (fetch)
origin    git@github.com:obse4/new-api.git (push)
upstream  https://github.com/QuantumNous/new-api.git (fetch)
upstream  https://github.com/QuantumNous/new-api.git (push)
```

### 2. 如果没有配置 upstream，添加它

```bash
# 添加上游仓库
git remote add upstream https://github.com/QuantumNous/new-api.git

# 验证配置
git remote -v
```

### 3. 确保本地代码是最新的

```bash
# 切换到主分支
git checkout main

# 拉取你的 fork 仓库的最新代码
git pull origin main
```

## 📥 日常同步流程

### 方法一：标准合并流程（推荐）

这是最安全和推荐的方式，适合大多数情况。

#### 步骤 1: 确保工作区干净

```bash
# 检查当前状态
git status

# 如果有未提交的更改，先提交或暂存
git add .
git commit -m "你的更改说明"

# 或者暂存更改（稍后可以恢复）
git stash
```

#### 步骤 2: 切换到主分支并更新

```bash
# 切换到主分支
git checkout main

# 拉取你的 fork 仓库的最新代码
git pull origin main
```

#### 步骤 3: 获取上游仓库的最新代码

```bash
# 从上游仓库获取最新代码（不合并）
git fetch upstream main
```

#### 步骤 4: 创建合并分支（推荐）

为了安全起见，建议在单独的分支上进行合并：

```bash
# 创建一个新的合并分支
git checkout -b merge-upstream-$(date +%Y%m%d)

# 或者使用更描述性的名称
git checkout -b merge-upstream-v1.x.x
```

#### 步骤 5: 合并上游代码

```bash
# 合并上游的 main 分支到当前分支
git merge upstream/main
```

#### 步骤 6: 解决冲突（如果有）

如果出现冲突，Git 会提示你。参考[冲突解决](#冲突解决)部分。

#### 步骤 7: 测试代码

合并后，务必进行测试：

```bash
# 构建项目
make linux

# 运行测试（如果有）
# go test ./...

# 检查是否有编译错误
go build -o new-api main.go
```

#### 步骤 8: 提交合并结果

```bash
# 如果合并成功且没有冲突
git add .
git commit -m "Merge upstream/main into main - $(date +%Y-%m-%d)"

# 如果有冲突已解决
git add .
git commit -m "Merge upstream/main: resolved conflicts - $(date +%Y-%m-%d)"
```

#### 步骤 9: 推送到你的 fork 仓库

```bash
# 推送到你的 fork 仓库
git push origin merge-upstream-$(date +%Y%m%d)

# 或者如果直接在 main 分支上合并
git push origin main
```

#### 步骤 10: 合并到主分支（如果使用了合并分支）

```bash
# 切换回 main 分支
git checkout main

# 合并合并分支
git merge merge-upstream-$(date +%Y%m%d)

# 推送到远程
git push origin main

# 删除本地合并分支（可选）
git branch -d merge-upstream-$(date +%Y%m%d)
```

### 方法二：使用 Rebase（高级）

如果你想要更线性的提交历史，可以使用 rebase：

```bash
# 确保在 main 分支
git checkout main

# 获取上游代码
git fetch upstream main

# Rebase 你的更改到上游代码之上
git rebase upstream/main

# 如果有冲突，解决后继续
git rebase --continue

# 推送到远程（需要强制推送）
git push origin main --force-with-lease
```

> ⚠️ **警告**: Rebase 会重写提交历史，如果已经推送到远程，需要强制推送。建议只在个人分支上使用。

## 🔧 冲突解决

### 识别冲突

当合并时出现冲突，Git 会显示类似信息：

```
Auto-merging path/to/file.go
CONFLICT (content): Merge conflict in path/to/file.go
Automatic merge failed; fix conflicts and then commit the result.
```

### 查看冲突文件

```bash
# 查看所有冲突文件
git status

# 查看冲突详情
git diff
```

### 解决冲突

#### 方法 1: 手动编辑（推荐）

1. 打开冲突文件，查找冲突标记：
   ```go
   <<<<<<< HEAD
   // 你的代码
   =======
   // 上游的代码
   >>>>>>> upstream/main
   ```

2. 编辑文件，保留需要的代码，删除冲突标记：
   ```go
   // 合并后的代码（保留你的修改和上游的改进）
   ```

3. 标记为已解决：
   ```bash
   git add path/to/file.go
   ```

#### 方法 2: 使用工具

```bash
# 使用 VS Code 解决冲突
code path/to/file.go

# 或使用其他合并工具
git mergetool
```

#### 方法 3: 选择保留哪一方的代码

```bash
# 完全保留你的版本
git checkout --ours path/to/file.go

# 完全保留上游的版本
git checkout --theirs path/to/file.go

# 标记为已解决
git add path/to/file.go
```

### 完成冲突解决

```bash
# 所有冲突解决后，继续合并
git commit -m "Merge upstream/main: resolved conflicts"
```

## ✅ 测试与验证

### 1. 编译检查

```bash
# 构建 Linux 版本
make linux

# 检查二进制文件是否生成
ls -lh new-api
```

### 2. 代码检查

```bash
# 运行 Go 代码检查（如果有配置）
go vet ./...

# 检查代码格式
gofmt -l .
```

### 3. 功能测试

```bash
# 启动本地服务测试
./new-api --port 3000

# 在另一个终端测试 API
curl http://localhost:3000/api/v1/status
```

### 4. 数据库迁移测试

如果上游有数据库迁移，确保 SQLite 数据库可以正常迁移：

```bash
# 备份现有数据库（如果有）
cp one-api.db one-api.db.backup

# 运行应用，检查迁移是否成功
./new-api --port 3000
```

## 🚀 部署上线

### 1. 创建发布标签（可选）

```bash
# 创建标签
git tag -a v1.x.x -m "Release v1.x.x - merged upstream"

# 推送到远程
git push origin v1.x.x
```

### 2. 使用部署脚本部署

```bash
# 确保部署配置正确
cat deploy.config

# 执行部署
./deploy.sh
```

### 3. 验证部署

```bash
# 检查服务状态
ssh root@your-server-ip "systemctl status new-api"

# 查看日志
ssh root@your-server-ip "journalctl -u new-api -n 50"
```

## 📝 最佳实践

### 1. 定期同步

- **建议频率**: 每周或每两周同步一次
- **最佳时机**: 上游发布新版本后
- **检查方式**: 关注上游仓库的 Releases 和 Commits

### 2. 提交信息规范

使用清晰的提交信息：

```bash
# 好的提交信息
git commit -m "Merge upstream/main: sync latest changes (v1.2.3)"
git commit -m "Merge upstream/main: resolved conflicts in model/user.go"

# 不好的提交信息
git commit -m "merge"
git commit -m "update"
```

### 3. 保留你的修改

在合并时，确保保留你的二次开发内容：

- 记录你修改的文件列表
- 在合并前备份重要更改
- 使用分支进行实验性更改

### 4. 测试清单

每次合并后，检查以下内容：

- [ ] 代码可以正常编译
- [ ] 没有明显的语法错误
- [ ] 数据库迁移正常（如果有）
- [ ] 核心功能正常工作
- [ ] 你的自定义功能仍然可用

### 5. 文档更新

如果上游有重要更新，记得更新相关文档：

```bash
# 检查是否有文档更新
git diff upstream/main -- docs/
```

## 🐛 常见问题

### Q1: 合并后代码无法编译

**解决方案**:
```bash
# 检查 Go 模块依赖
go mod tidy
go mod download

# 检查是否有语法错误
go build -o new-api main.go
```

### Q2: 数据库迁移失败

**解决方案**:
```bash
# 备份数据库
cp one-api.db one-api.db.backup

# 查看迁移文件
ls -la bin/migration_*.sql

# 手动运行迁移（如果需要）
sqlite3 one-api.db < bin/migration_v0.x-v0.y.sql
```

### Q3: 冲突太多，不知道如何解决

**解决方案**:
1. 先备份你的更改：`git stash`
2. 完全使用上游版本：`git checkout --theirs .`
3. 然后重新应用你的更改：`git stash pop`
4. 手动解决剩余的冲突

### Q4: 合并后功能不工作

**解决方案**:
1. 检查是否有环境变量变化
2. 查看上游的 CHANGELOG 或 Release Notes
3. 检查配置文件是否需要更新
4. 回滚到上一个稳定版本：`git reset --hard <previous-commit>`

### Q5: 如何查看上游的更新内容

```bash
# 查看上游的提交历史
git log upstream/main --oneline -10

# 查看具体的更改
git diff main..upstream/main

# 查看某个文件的更改
git diff main..upstream/main -- path/to/file.go
```

### Q6: 如何回滚合并

```bash
# 如果还没有推送
git reset --hard HEAD~1

# 如果已经推送
git revert -m 1 HEAD
git push origin main
```

## 📚 快速参考命令

### 完整同步流程（一键脚本）

创建一个脚本 `sync-upstream.sh`:

```bash
#!/bin/bash
set -e

echo "🔄 开始同步上游代码..."

# 1. 确保工作区干净
if [ -n "$(git status --porcelain)" ]; then
    echo "❌ 工作区有未提交的更改，请先提交或暂存"
    exit 1
fi

# 2. 切换到主分支
git checkout main
git pull origin main

# 3. 获取上游代码
git fetch upstream main

# 4. 创建合并分支
BRANCH_NAME="merge-upstream-$(date +%Y%m%d)"
git checkout -b $BRANCH_NAME

# 5. 合并上游代码
echo "📥 合并上游代码..."
git merge upstream/main || {
    echo "⚠️  检测到冲突，请手动解决后运行:"
    echo "   git add ."
    echo "   git commit -m 'Merge upstream/main: resolved conflicts'"
    echo "   git checkout main"
    echo "   git merge $BRANCH_NAME"
    echo "   git push origin main"
    exit 1
}

# 6. 测试构建
echo "🔨 测试构建..."
make linux || {
    echo "❌ 构建失败，请检查错误"
    exit 1
}

# 7. 提交合并
git add .
git commit -m "Merge upstream/main - $(date +%Y-%m-%d)"

# 8. 合并到主分支
git checkout main
git merge $BRANCH_NAME

# 9. 推送到远程
echo "📤 推送到远程仓库..."
git push origin main

# 10. 清理
git branch -d $BRANCH_NAME

echo "✅ 同步完成！"
```

使用方式：
```bash
chmod +x sync-upstream.sh
./sync-upstream.sh
```

## 🎯 总结

1. **定期同步**: 建议每周或每两周同步一次
2. **使用分支**: 在单独分支上合并，测试后再合并到主分支
3. **充分测试**: 合并后务必进行编译和功能测试
4. **记录更改**: 保留你的修改记录，避免丢失
5. **及时部署**: 测试通过后及时部署到生产环境

---

**最后更新**: 2025-01-02
**维护者**: 项目维护团队

